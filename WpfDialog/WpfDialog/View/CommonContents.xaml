<UserControl x:Class="WpfDialog.View.CommonContents"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfDialog.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Orientation="Vertical">
            <TextBlock x:Name="TitleText" Text="Title" FontSize="32" Margin="10"/>
            <TextBlock x:Name="SubTitleText" Text="{Binding SubTitle}" FontSize="24" Margin="10"/>
            <TextBlock x:Name="DescriptionText" Text="{Binding Description}" FontSize="20" Margin="10"/>
        </StackPanel>
    </Grid>
</UserControl>
